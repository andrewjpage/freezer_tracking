<%= form_for(@building_area) do |f| %>
  <% if @building_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_area.errors.count, "error") %> prohibited this building_area from being saved:</h2>

      <ul>
      <% @building_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
